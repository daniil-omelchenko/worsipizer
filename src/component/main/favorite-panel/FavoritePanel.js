import React, { Component } from 'react';
import './FavoritePanel.css'

var FavoritePanel = React.createClass({
    render: function() {
        return (
            <div className="favorite-panel">
                It is FavoritePanel
            </div>
        );
    },
});

export default FavoritePanel;